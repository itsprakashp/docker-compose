version: "3.8"
services:
  adguardhome-sync:
    image: "linuxserver/adguardhome-sync:0.6.6"
    container_name: "adguardhome-sync"
    environment:
      - "PUID=${PUID}"
      - "PGID=${PGID}"
      - "TZ=${TZ}"
    volumes:
      - "${PATH}:/config"
    ports:
      - "${PORT}:8080"
    restart: "unless-stopped"
